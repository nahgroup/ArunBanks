using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.Runtime.InteropServices;
using System;

public class JavaImplimentation : MonoBehaviour
{
    /*[DllImport("__Internal")]
    private static extern void Complete();

    [DllImport("__Internal")]
    private static extern double TheInput();

    // Start is called before the first frame update
    void Start()
    {
        GameObject.Find("Score").GetComponent<FallingManager>().DepositAmount = TheInput();
    }

    // Update is called once per frame
    void Update()
    {
        if(gameObject.GetComponent<SpriteRenderer>().color.a == 1)
        {
            Complete();
        }
    }*/
}
